# API

- Public HTTP API is defined in [`contracts/api/openapi.yaml`](../contracts/api/openapi.yaml) (OpenAPI 3.0).
- Event and run-file JSON payloads are documented under [`contracts/README.md`](../contracts/README.md).
- Operational notes and auth flows will be summarized here once the backend ships endpoints.
