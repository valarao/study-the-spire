# Release process

- **Mod**, **API**, and **web** release channels and versioning will be defined once CI and deploy paths exist.
- Mod releases will likely align with GitHub Actions under `.github/workflows/` (see build plan).
