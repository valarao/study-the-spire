# Shared REST settings for all environments.
clerk {
  jwksUrl = ${?CLERK_JWKS_URL}
  issuer  = ${?CLERK_ISSUER}
}

rest {
  connector {
    host = "127.0.0.1"
    port = "8080"
  }
  plugins {
    defaultHeaders {
      serverName = "study-the-spire-api"
    }
  }
}
